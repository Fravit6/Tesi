import 'package:flutter/material.dart';

/*
 * Pagina di transizione per i caricamenti
 */
class SplashScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Loading...'),
      ),
    );
  }
}
